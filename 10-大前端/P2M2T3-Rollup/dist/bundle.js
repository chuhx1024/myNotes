(function () {
    'use strict';

    const log = msg => {
        console.log('----------------INFO------------------');
        console.log(msg);
        console.log('--------------------------------------');
    };

    var name = "rollup";
    var version = "1.0.0";

    log(mag);
    log(name);
    log(version);

}());
